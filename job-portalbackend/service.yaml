apiVersion: v1
kind: Service
metadata:
  name: job-portal-backend-service
spec:
  type: NodePort
  selector:
    app: job-portal-backend
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30080